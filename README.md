# Lost Dungeon

一个 Canvas + 原生 JavaScript 的像素地下城肉鸽小游戏。

## GitHub Pages 部署

1. 新建一个 GitHub 仓库。
2. 把这个文件夹里的 `index.html` 上传到仓库根目录。
3. 进入仓库 `Settings` -> `Pages`。
4. Source 选择 `Deploy from a branch`。
5. Branch 选择 `main` 和 `/root`。
6. 保存后等待 GitHub 生成网址。

也可以同时上传 `lost-dungeon.html`，它只是同一游戏的备用文件名。
